# Zameel-specific R8 rules.
# Flutter and its plugins provide their own consumer rules.

# Keep model/serialization metadata used by reflection-based plugins.
-keepattributes *Annotation*
-keepattributes Signature
