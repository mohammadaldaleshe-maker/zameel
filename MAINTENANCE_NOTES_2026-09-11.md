# Zameel maintenance — 2026-09-11

## Applied
- Repaired the modular Dart library structure.
- Added `part of '../main.dart';` to all six files included by `lib/main.dart`:
  - `auth_app.dart`
  - `university.dart`
  - `home_feed.dart`
  - `arc_menu.dart`
  - `drawer_profile.dart`
  - `posts_media.dart`
- No UI layouts, feature logic, routes, database schema, assets, or feature behavior were intentionally changed.

## Required local validation
Run from the project root:

```powershell
flutter clean
flutter pub get
flutter analyze
flutter test
flutter build appbundle --release
```

`pubspec.lock` was intentionally not hand-edited because the exact dependency graph should be regenerated by the installed Flutter/Dart toolchain.

## Important release checks
- Verify Firebase `google-services.json` is supplied through the intended release/CI mechanism.
- Verify the release signing key is configured before publishing to Google Play.
- Confirm the final app version in `pubspec.yaml` before creating the production AAB.
